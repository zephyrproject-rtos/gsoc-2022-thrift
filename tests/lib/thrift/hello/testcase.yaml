common:
  tags: thrift cpp
  # FIXME: zephyrproject-rtos/zephyr#45100
  arch_exclude: posix
tests:
  thrift.hello.newlib:
    filter: TOOLCHAIN_HAS_NEWLIB == 1
    toolchain_exclude: xcc
    platform_exclude: qemu_x86 qemu_leon3 qemu_malta qemu_malta_be qemu_riscv64
    arch_exclude: arc xtensa
    tags: newlib
#  thrift.hello.native:
#    arch_allow: posix
#    tags: native
  thrift.hello.arcmwdtlib:
    toolchain_allow: arcmwdt
    platform_allow: nsim_hs nsim_em
    tags: arc
    extra_configs:
      - CONFIG_ARCMWDT_LIBC=y
